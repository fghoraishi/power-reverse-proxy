# Define parameters.
#
# You can change the cos_endpoint parameter after instllation, the script will then just update the nginx.conf file with new info and restart nginx
############################ Input parametres ######################


# Change or leave as is
# NOTE: you must define the COS endpoint as https://<endpoint> format.
# For VPC VSI you must use direct Endpoint. For IBM Classic VSI, use private Endpoints.

cos_endpoint="https://s3.direct.us-south.cloud-object-storage.appdomain.cloud"

# Optional parameters:
password="XERF123RERSSSAXVBR"
encrypt="sha256"
rsa=2048


